[project]
name = "camerino-color"
version = "0.1.0"
description = "Deterministic color science for Camerino: seasonal color analysis and Delta-E matching"
requires-python = ">=3.12"
dependencies = []

[build-system]
requires = ["hatchling"]
build-backend = "hatchling.build"

[tool.hatch.build.targets.wheel]
packages = ["src/camerino_color"]
