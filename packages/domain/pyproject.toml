[project]
name = "camerino-domain"
version = "0.1.0"
description = "Camerino pure domain entities and logic (no I/O)"
requires-python = ">=3.12"
dependencies = []

[build-system]
requires = ["hatchling"]
build-backend = "hatchling.build"

[tool.hatch.build.targets.wheel]
packages = ["src/camerino"]
