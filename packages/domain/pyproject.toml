[project]
name = "camerino-domain"
version = "0.1.0"
description = "Framework-free domain entities and services for Camerino"
requires-python = ">=3.12"
dependencies = []

[build-system]
requires = ["hatchling"]
build-backend = "hatchling.build"

[tool.hatch.build.targets.wheel]
packages = ["src/camerino_domain"]
